<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    tools:context="com.tdtf.yuklyn.coldchaintransport.MainActivity">

    <item android:id="@+id/btn_clear"
        app:showAsAction="ifRoom"
        android:icon="@drawable/btn_clear"
        android:title="@string/clear" />
    <item android:id="@+id/btn_print"
        android:title="@string/print"
        android:icon="@drawable/btn_print"
        app:showAsAction="ifRoom" />
    <item android:id="@+id/btn_refresh"
        android:title="@string/refresh"
        android:icon="@drawable/btn_refresh"
        app:showAsAction="ifRoom" />
</menu>