<?xml version="1.0" encoding="utf-8"?>

<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content">

    <LinearLayout
        android:layout_alignParentStart="true"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:orientation="vertical">

        <TextView
            android:id="@+id/tv_deviceName"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textSize="@dimen/text_size_small" />
        <TextView
            android:id="@+id/tv_deviceSID"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textSize="@dimen/text_size_small" />

    </LinearLayout>

    <ProgressBar
        android:id="@+id/progress_change"
        android:layout_alignParentEnd="true"
        android:layout_width="@dimen/image_size_normal"
        android:layout_height="@dimen/image_size_normal" />
    <ImageButton
        android:id="@+id/imgBtn_set"
        android:layout_alignParentEnd="true"
        android:layout_width="@dimen/image_size_normal"
        android:layout_height="@dimen/image_size_normal"
        android:focusable="false"
        android:background="@drawable/btn_set"/>
    <ImageButton
        android:id="@+id/imgBtn_change"
        android:layout_toLeftOf="@+id/imgBtn_delete"
        android:layout_width="@dimen/image_size_normal"
        android:layout_height="@dimen/image_size_normal"
        android:focusable="false"
        android:background="@drawable/btn_change"/>
    <ImageButton
        android:id="@+id/imgBtn_delete"
        android:layout_toLeftOf="@+id/imgBtn_set"
        android:layout_width="@dimen/image_size_normal"
        android:layout_height="@dimen/image_size_normal"
        android:focusable="false"
        android:background="@drawable/btn_delete"/>

</RelativeLayout>

